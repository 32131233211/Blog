---
/* ============================================================
   RUTA "/etapas/etapa3" — PENDIENTE DE CONTENIDO
   ============================================================
   La ruta ya está enlazada desde el menú (ROUTES.entrada3), pero
   el contenido de la entrada todavía no está definido. Mientras
   tanto, se muestra la pantalla compartida "en construcción" en
   vez de dejar que Astro devuelva un 404.

   Cuando la entrada tenga contenido real, esta página se
   reemplaza por su propia composición de _sections/, igual que
   src/pages/etapas/etapa1/ y etapa2/.
   ============================================================ */
import BaseLayout from "../../../layouts/BaseLayout.astro";
import EncabezadoPagina from "../../../components/EncabezadoPagina.astro";
import PantallaEnConstruccion from "../../../components/PantallaEnConstruccion.astro";
---

<BaseLayout titulo="Entrada 3 — Puente Digital">
  <EncabezadoPagina etiqueta="Entrada 3" titulo="Entrada 3" />
  <PantallaEnConstruccion />
</BaseLayout>
